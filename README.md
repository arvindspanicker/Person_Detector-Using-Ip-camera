#Person Detector Using IP-Camera and Tensorflow

##Installing Dependencies
* pip install opencv-python
* pip install numpy
* pip install Cython
* pip install tensorflow


##To run 
<p>Run the python file : detect_person.py</p>
